# leads-app-pg
Hosted UI for the Shine Property Services **Prince George** lead tracker.
UI only — data loads at runtime from the gated Supabase `leads-board` function (loc=pg).
Open with `?key=PASSCODE`. Rebuild via `shine-leads/scripts/build-pages.ps1 -Loc pg -Label "Prince George"`.
